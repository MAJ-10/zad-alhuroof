# زاد الحروف

بنك أسئلة ثقافية مرتّبة على حروف الهجاء، إجابة كل سؤال تبدأ بحرفه.
إعداد مشرفي المرحلة الابتدائية في **نادي جيل**.

**١٣٩٤ سؤالاً · ٢٨ حرفاً**

الموقع: `https://<اسم-حسابك>.github.io/zad-alhuroof`

---

## الاستعمال

اضغط حرفاً من الشبكة فيظهر سؤال من أسئلته. تحته زرّان:

- **عرض الإجابة** — تنزل خانة الإجابة تحت السؤال، والضغط مرة أخرى يخفيها.
- **تبديل السؤال** — سؤال آخر من الحرف نفسه.

الضغط على الحرف النشط نفسه يبدّل السؤال أيضاً، و**حرف عشوائي** ينتقل إلى حرف آخر.
زر الشمس/القمر في الأعلى يبدّل بين الوضع الفاتح والليلي، ويُحفظ اختيارك مع آخر حرف
فتحته في متصفحك.

---

## الملفات

| الملف | ما هو |
|---|---|
| `index.html` | الموقع كاملاً — الأسئلة والشعار والخط بداخله |
| `zad-alhuroof-questions.json` | بنك الأسئلة منفصلاً، للمراجعة والتعديل |
| `og.png` | صورة معاينة الرابط عند مشاركته |
| `icon-180.png` · `icon-512.png` | أيقونة بلاطة حرف الحاء للاختصار على الجوال |
| `icon.svg` | الأيقونة متجهاً (مضمَّنة أصلاً داخل `index.html`) |
| `OFL.txt` | رخصة خط IBM Plex Sans Arabic |

> صورة المعاينة تحتاج رابطاً مطلقاً، فوسوم `og:` في `index.html` مكتوب فيها
> `https://maj-10.github.io/zad-alhuroof/`. لو غيّرت اسم المستودع أو النطاق،
> غيّرها معه وإلا ظهر الرابط بلا صورة.

ملف واحد مكتفٍ بذاته: لا سيرفر، ولا خطوة بناء، ولا اتصال بالإنترنت. تقدر تفتحه
بالضغط عليه مرتين من جهازك مباشرة.

---

## تعديل الأسئلة

الأسئلة مكتوبة داخل `index.html` في المتغيّر `RAW` قرب أول السكربت، بنفس بنية
ملف الـJSON:

```json
[
  { "letter": "ا",
    "items": [
      { "q": "ما أكبر قارات العالم مساحةً؟", "a": "آسيا" }
    ]
  }
]
```

- `letter` أحد الحروف الثمانية والعشرين. **الألف مفتاحها `ا` والهاء مفتاحها `ه`**،
  وتُعرضان في الواجهة `أ` و`هـ`.
- `q` نص السؤال، و`a` إجابته.

عدّل في `index.html` مباشرة، واحفظ التعديل نفسه في ملف الـJSON حتى يبقى مرجعاً
مقروءاً.

---

## إعدادات في أعلى السكربت

```js
const SPLASH = 'fall';
```

ستارة الافتتاح. الخيارات: `'fall'` حروف تتساقط · `'tiles'` بلاطات تتقلب ·
`'write'` كتابة متصلة · `'gather'` تتجمّع من التبعثر.

---

## النشر

**GitHub Pages:** Settings ← Pages ← Source: Deploy from a branch ← `main` / `(root)`.
يشتغل خلال دقيقة أو دقيقتين، وكل `git push` بعدها يحدّثه.

**تجربة سريعة بلا حساب:** اسحب المجلد إلى `app.netlify.com/drop`.

---

## أصل المحتوى

الأسئلة مستخرَجة آلياً من ملف PDF أعدّه مشرفو النادي. كان الملف يخزّن النص بخط
Traditional Arabic بترميز ناقص: الحروف المركّبة تخرج معكوسة، والأرقام مقلوبة،
والمسافات تقع داخل الكلمات. فالاستخراج تمّ على مستوى الرسم لا النص، مع معايرة
الفراغ على قياسه الحقيقي. راجَعتُ عيّنات مقابل صور الصفحات، لكن تبقى نسبة خطأ
صغيرة محتملة — إن وجدت خطأً صحّحه في الملفين.

---

## الرخص

الكود لنادي جيل. الأسئلة من إعداد مشرفي المرحلة الابتدائية في النادي.
الخط IBM Plex Sans Arabic © IBM Corp. برخصة SIL Open Font License 1.1 (`OFL.txt`).
